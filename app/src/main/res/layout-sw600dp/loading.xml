<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/llPopup"
    android:layout_width="fill_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="25dp">

    <ImageView
        android:id="@+id/imgeView"
        android:layout_width="@dimen/loder_height_width"
        android:layout_height="@dimen/loder_height_width"
        android:background="#000090"
        android:contentDescription="@string/imagestatedescription"
        android:visibility="gone" />

    <TextView
        android:id="@+id/tvLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:maxLines="1"
        android:singleLine="true"
        android:text="@string/loading"
        android:textColor="#fff"
        android:textSize="@dimen/activity_large_textSize"
        android:visibility="visible" />

</LinearLayout>